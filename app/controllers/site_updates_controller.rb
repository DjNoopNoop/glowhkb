class SiteUpdatesController < ApplicationController
  # Allow the updates page to be publicly visible.
  # Previously `require_login` was applied to `index` which prevented public access.

  def index
    # placeholder page for future SiteUpdate index
  end
end
