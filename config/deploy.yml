service: glowhkb
image: djnoopnoop/glowhkb

servers:
  web:
    - 167.172.27.4

proxy:
  ssl: false
  host: glowhkb.net
  app_port: 3000

registry:
  server: localhost:5555

builder:
  arch: amd64

env:
  clear:
    DB_HOST: glowhkb-db
    DB_PORT: 5432
    DB_USERNAME: postgres
    APP_HOST: glowhkb.net
    DEFAULT_FROM_EMAIL: support@glowhkb.net
    SMTP_ADDRESS: smtp.mailgun.org
    SMTP_PORT: 587
    SMTP_DOMAIN: glowhkb.net
    SMTP_AUTH: plain
    SMTP_ENABLE_STARTTLS_AUTO: "true"
    SMTP_OPEN_TIMEOUT: 5
    SMTP_READ_TIMEOUT: 5
    RECAPTCHA_SITE_KEY: 6LfyWnctAAAAADY5gn4N6MNYHBJC6bK5Fs-aMoMQ
  secret:
    - RAILS_MASTER_KEY
    - SECRET_KEY_BASE
    - DB_PASSWORD
    - MAPBOX_ACCESS_TOKEN
    - SMTP_USERNAME
    - SMTP_PASSWORD
    - MAILGUN_API_KEY
    - MAILGUN_DOMAIN
    - RECAPTCHA_SECRET_KEY

accessories:
  db:
    image: postgres:16
    host: 167.172.27.4
    env:
      clear:
        POSTGRES_DB: glowhkb_production
        POSTGRES_USER: postgres
      secret:
        - POSTGRES_PASSWORD
    directories:
      - local: /var/lib/glowhkb/postgres
        remote: /var/lib/postgresql/data
        owner: "999:999"